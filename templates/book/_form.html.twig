{{ form_start(form, {'attr': {'class': 'form form--its-book'}}) }}
    {{ form_widget(form) }}

        {% if book.image %}
            <img class="" style="width:100px; height:100px;" src="{{ asset(public ~ book.image.filename) }}" alt="">
        {% endif %}
    <button class="btn form__btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
