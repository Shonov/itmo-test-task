{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <section class="section create-form">
        <div class="section__content create-form__content">
            <h1 class="section__title">Edit Book</h1>
            {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

            <div>
                {{ include('book/_delete_form.html.twig') }}
                <a href="{{ path('book_index') }}" class="section__create-link">back to list</a>
            </div>
        </div>
    </section>
{% endblock %}
